server.port=8080

logging.level.org.springframework=DEBUG

# mybatis config
mybatis.mapper-locations = classpath:mapper/*Mapper.xml
#mybatis.config-location = classpath:mybatisConfig.xml
mybatis.type-aliases-package = com.join.learn.ssm.model

#database connection config
spring.datasource.driver-class-name= com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/ssm?useUnicode=true&characterEncoding=utf-8
spring.datasource.username = root
spring.datasource.password = 123456

spring.druid.url=
spring.druid.username=
spring.druid.password
